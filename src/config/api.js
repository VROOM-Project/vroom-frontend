'use strict';

module.exports = {
  tileLayer: 'http://{s}.tile.osm.org/{z}/{x}/{y}.png',
  host: 'http://localhost',
  port: '3000',
  maxTaskNumber: 100,
  overpassEndpoint: 'https://overpass-api.de/api/interpreter'
};
